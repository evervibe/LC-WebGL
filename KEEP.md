du This directory contains large legacy assets that are not ideal for Git history.

Policy:
- Keep only small sample assets under `public/3rdparty/` in the repository.
- For full large asset sets, prefer a release artifact, LFS, or external storage and document the location here.

If you must check-in a large file, explain the reason and the size in a PR description.
